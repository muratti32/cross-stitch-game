# Leave VITE_API_URL empty to enable the "API not configured" warning banner
# on the Account Deletion page. Fill it in when the backend deletion endpoint
# is implemented.
VITE_API_URL=

# URL of the Operator Console (admin-console Next.js app).
VITE_ADMIN_CONSOLE_URL=http://localhost:3001
